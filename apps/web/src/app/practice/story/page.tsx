"use client";
import { TypeHub } from "@/components/practice/TypeHub";

export default function Page() {
  return <TypeHub types="storytelling_prompt" title="Storytelling" description="Every prompt suggests a framework. Practice the structure until it becomes invisible." groupBy="category" />;
}
